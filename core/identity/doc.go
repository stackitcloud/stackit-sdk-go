// Package identity contains modular token providers and composition primitives.
//
// This package exposes a minimal contract via TokenProvider and includes
// initial providers for service account key flow, workload identity federation,
// and instance metadata tokens, plus a composable Chain.
package identity
